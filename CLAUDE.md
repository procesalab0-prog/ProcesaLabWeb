# CLAUDE.md

Ver `AGENTS.md` en la raíz de este repo — ahí está toda la documentación
del proyecto (arquitectura, almacenamiento en Vercel Blob, límite de
funciones serverless, estructura de `/privado.html`, flujo de git). Se
mantiene en `AGENTS.md` en vez de aquí porque lo comparten Claude Code y
Codex, que trabajan este repo en ramas separadas.
